kind: core/block-type
metadata:
  name: blockware/block-type-service
  title: Service

spec:
  schema:
    type: object
    properties:
      target:
        $ref: core/language-target-reference
      entities:
        $ref: core/entity-list
      consumers:
        $ref: core/resource-list
      providers:
        $ref: core/resource-list
    required:
      - target
